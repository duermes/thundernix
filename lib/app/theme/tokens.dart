import 'package:flutter/material.dart';

class AppTokens {
  // Base
  static const blackDeep = Color(0xFF000007);
  static const bg = Color(0x80F5F5FF); // 50% opacidad
  static const btnDark50 = Color(0x80000007); // 50% opacidad
  static const notifySurface = Color(0xFFEFEEFF);
  static const danger = Color(0xFFFF0000);

  // Superficies y bordes
  static const surface = Color(0xFFFFFFFF);
  static const border = Color(0x1F000007); // ~12% opacidad

  // Texto
  static const textPrimary = blackDeep;
  static const textSecondary = Color(0x99000007); // 60% opacidad

  // Dimensiones
  static const r12 = 12.0;
  static const s8 = 8.0, s12 = 12.0, s16 = 16.0, s20 = 20.0;

  // Alturas típicas
  static const inputH = 48.0;
  static const circleBtn = 56.0;
  static const bottomBarH = 72.0;
}
